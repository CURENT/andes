# FORMAT
# Put your extra requirements here in the following format
#
# package[version_required] # tag1, tag2, ...
#
# Allow at least one space between the package name/version and '#'
#
# Only one `#` is allowed per line. Lines starting with `#` are ignored.

# Development dependencies with enforced version constraints
coverage>=6.0,<8.0 #                 dev
pytest>=7.4,<8.0 #                   dev
flake8>=6.0,<8.0 #                   dev
sphinx>=5.0,<8.0 #                   dev, doc
ipython>=8.0,<10.0 #                 dev
numpydoc>=1.4,<2.0 #                 dev, doc
sphinx-copybutton>=0.5,<1.0 #        dev, doc
sphinx-panels>=0.4,<1.0 #            dev, doc
pydata-sphinx-theme>=0.13,<1.0 #     dev, doc
myst-parser>=0.15,<3.0 #             dev, doc
myst-nb>=0.13,<2.0 #                 dev, doc
mdit-py-plugins>=0.3.1,<0.4 #        dev, doc
pre-commit>=3.0,<4.0 #               dev

# Interoperability dependencies
pandapower>=2.10,<3.0 #              interop

# Web interface dependencies
# below work around a bug with tqdm bar with ipywidgets 8.0.1
ipywidgets==7.7.* #                  web
